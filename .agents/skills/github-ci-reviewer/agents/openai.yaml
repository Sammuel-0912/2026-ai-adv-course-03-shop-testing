interface:
  display_name: "GitHub CI Reviewer"
  short_description: "Monitor CI, repair failures, and open pull requests"
  default_prompt: "Use $github-ci-reviewer to monitor the latest pushed commit and handle its CI result."
